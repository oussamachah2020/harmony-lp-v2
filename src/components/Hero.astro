---
import DashboardImage from "../assets/img/dashboard.png"
---
<header class="relative overflow-hidden bg-white pt-20 pb-16 sm:pt-28 sm:pb-20">
	<!-- ★ Background Layer -->
	<div class="absolute inset-0 -z-10" aria-hidden="true">
	  <!-- Radial Gradient -->
	  <div class="absolute inset-0 bg-gradient-radial from-white via-white to-[#eef0ff]"></div>
  
	  <!-- Blurred Blobs -->
	  <div class="absolute -top-24 -left-24 w-72 h-72 bg-[#4250D8] opacity-15 rounded-full blur-3xl animate-pulse-slow"></div>
	  <div class="absolute -bottom-24 -right-24 w-96 h-96 bg-[#4250D8] opacity-15 rounded-full blur-3xl animate-pulse-slow animation-delay-2000"></div>
  
	  <!-- Subtle Dot Pattern -->
	  <div class="absolute inset-0 opacity-5"
		   style="background-image:radial-gradient(#4250D8 1px,transparent 1px);background-size:28px 28px"></div>
	</div>
  
	<!-- ★ Main Container -->
	<div class="mx-auto w-[92%] sm:w-[90%] md:w-[85%] lg:w-[80%] px-4 sm:px-6 lg:px-8">
	  <div class="flex flex-col-reverse lg:flex-row items-center gap-10">
		
		<!-- ▸ Text Content -->
		<div class="w-full lg:w-1/2 text-center lg:text-left">
		  <span class="inline-block bg-[#4250D8]/10 px-3 py-1 text-xs sm:text-sm font-semibold tracking-wide text-[#4250D8] rounded-full">
			Introducing
		  </span>
  
		  <h1 class="mt-3 font-extrabold leading-tight tracking-tight text-gray-900">
			<span class="block text-4xl sm:text-5xl md:text-6xl">Harmony</span>
			<span class="block text-[#4250D8]">Business&nbsp;Management</span>
		  </h1>
  
		  <p class="mt-4 text-base sm:text-lg md:text-xl text-gray-600 max-w-2xl mx-auto lg:mx-0">
			The all-in-one solution for companies to manage projects, tasks, payrolls, schedules and more.
			Streamline operations and boost productivity with Harmony.
		  </p>
  
		  <!-- CTA Buttons -->
		  <div class="mt-8 flex flex-col sm:flex-row sm:justify-center lg:justify-start gap-4">
			<a href="#"
			   class="inline-flex items-center justify-center px-6 py-3 text-base font-semibold rounded-md text-white bg-[#4250D8] hover:bg-[#3340b4] transition-shadow shadow-lg hover:shadow-xl">
			  Get started
			  <svg xmlns="http://www.w3.org/2000/svg" class="ml-2 h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
				<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
			  </svg>
			</a>
			<a href="#"
			   class="inline-flex items-center justify-center px-6 py-3 text-base font-semibold rounded-md text-[#4250D8] bg-white ring-1 ring-inset ring-[#4250D8]/20 hover:bg-gray-50 transition-shadow shadow-sm hover:shadow-md">
			  Live demo
			  <svg xmlns="http://www.w3.org/2000/svg" class="ml-2 h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
				<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 10l4.553-2.276A1 1 0 0121 8.618v6.764a1 1 0 01-1.447.894L15 14" />
			  </svg>
			</a>
		  </div>
		</div>
  
		<!-- ▸ Image -->
		<div class="w-full lg:w-1/2">
		  <div class="relative mx-auto max-w-md sm:max-w-lg md:max-w-xl lg:max-w-none">
			<!-- Decorative shapes -->
			<div class="absolute -top-4 -right-4 w-16 h-16 bg-[#4250D8] opacity-20 rounded-lg rotate-12" aria-hidden="true"></div>
			<div class="absolute -bottom-4 -left-4 w-20 h-20 bg-[#4250D8] opacity-10 rounded-full" aria-hidden="true"></div>
  
			<!-- Image with Glass Card -->
			<div class="relative rounded-xl overflow-hidden shadow-2xl backdrop-blur-sm bg-white/30 p-1 sm:p-2">
			  <img
				src={DashboardImage.src}
				alt="Harmony dashboard preview"
				loading="eager"
				class="w-full h-auto rounded-lg"
			  />
  
			  <!-- Badge -->
			  <span class="absolute top-3 right-3 bg-white/80 backdrop-blur px-3 py-1 text-xs font-medium text-gray-700 rounded-full shadow-sm">
				Dashboard Preview
			  </span>
			</div>
		  </div>
		</div>
  
	  </div>
	</div>
  
	<!-- ★ Animations -->
	<style>
	  @keyframes pulse-slow {
		0%, 100% { transform: scale(1); opacity: 0.15; }
		50%      { transform: scale(1.08); opacity: 0.25; }
	  }
	  .animate-pulse-slow {
		animation: pulse-slow 10s ease-in-out infinite;
	  }
	  .animation-delay-2000 {
		animation-delay: 2s;
	  }
	  /* Add in tailwind.config.js:
	  theme: {
		extend: {
		  backgroundImage: {
			'gradient-radial': 'radial-gradient(var(--tw-gradient-stops))',
		  }
		}
	  }
	  */
	</style>
  </header>
  